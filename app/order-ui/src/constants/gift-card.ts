export enum GiftCardStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum GiftCardType {
  SELF = 'self_use',
  GIFT = 'send_to_friend',
}

export enum ReceiverField {
  PHONE = 'phone',
  NOTE = 'note',
  QUANTITY = 'quantity',
}
